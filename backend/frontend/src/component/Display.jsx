import {Container} from "@mui/material";

export default function Display() {
    const STYLE = {
        backgroundColor: "#444f", borderRadius: 5
    }

    return (<Container maxWidth="xl" sx={STYLE}>
        INDEX
        </Container>);
}